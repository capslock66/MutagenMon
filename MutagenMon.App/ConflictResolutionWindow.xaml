<Window x:Class="MutagenMon.App.ConflictResolutionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MutagenMon: resolve file conflict" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Icon="pack://siteoforigin:,,,/Assets/Icons/app_icon_monitor.ico">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="FileNameText" Grid.Row="0" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,12" />

        <TextBlock x:Name="AlphaInfoText" Grid.Row="1" TextWrapping="Wrap" FontFamily="Consolas" Margin="0,0,0,8" />
        <TextBlock x:Name="BetaInfoText" Grid.Row="2" TextWrapping="Wrap" FontFamily="Consolas" Margin="0,0,0,12" />

        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <RadioButton x:Name="VisualMergeOption" Content="Visual merge" GroupName="ResolutionChoice" Margin="0,0,0,6" />
            <RadioButton x:Name="AWinsOption" Content="A wins" GroupName="ResolutionChoice" Margin="0,0,0,6" />
            <RadioButton x:Name="BWinsOption" Content="B wins" GroupName="ResolutionChoice" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="90" IsDefault="True" Click="OnOkClick" />
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Margin="8,0,0,0" IsCancel="True" Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
