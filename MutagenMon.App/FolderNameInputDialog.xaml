<Window x:Class="MutagenMon.App.FolderNameInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MutagenMon: New folder" Width="360" SizeToContent="Height"
        MinWidth="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Folder name" />
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,2" TextChanged="OnNameChanged" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Width="90" IsCancel="True" />
            <Button x:Name="OkButton" Content="OK" Width="90" Margin="8,0,0,0" IsDefault="True" IsEnabled="False" Click="OnOkClick" />
        </StackPanel>
    </Grid>
</Window>
